#include<iostream>
using namespace std;

int main(){
    //for storing the collection of values we need data structures
    //STL is a library in C++ that provides a lot of data structures and algorithms
    //STL is a part of C++ standard library
    //STL has 4 components: Algorithms, Containers, Functions, Iterators
    // The probelm with array is that we need to know the size of the array before hand, but in STL we can change the size of the array at runtime
    // for diagram prefer "Intro_02.jpg"
    return 0;
}